/**
 * Basic Data Structures: Use an Array to Store a Collection of Data
 */

// one dimensional array example:
let simpleArray = ['one', 2, 'three', true, false, undefined, null];

// array.length example:
console.log(simpleArray.length);

// multidimensional array example:
let complexArray = [
  [
    {
      one: 1,
      two: 2
    },
    {
      three: 3,
      four: 4
    }
  ],
  [
    {
      a: "a",
      b: "b"
    },
    {
      c: "c",
      d: "d"
    }
  ]
];

// using array.length on multidimensional array example:
console.log(complexArray.length);

// completing the challenge

let yourArray = ['Ryan', 49, false, 'SLC', 84107];